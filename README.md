# NPS Mini‑app (React + Vite + Vercel) — version corrigée
- Point clé: dans `index.html`, le script utilise un chemin **relatif** `./src/main.jsx`.
- Déploie sur Vercel avec build `vite build` et output `dist`.
